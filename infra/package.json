{
  "name": "@apoth/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "diff": "cdk diff",
    "diff:staging": "AWS_PROFILE=apoth-staging CDK_DEFAULT_ACCOUNT=329425487030 CDK_DEFAULT_REGION=us-east-1 cdk diff --context stage=staging Apoth-staging-ServerlessPlatform",
    "deploy:staging": "AWS_PROFILE=apoth-staging CDK_DEFAULT_ACCOUNT=329425487030 CDK_DEFAULT_REGION=us-east-1 cdk deploy --context stage=staging Apoth-staging-ServerlessPlatform --require-approval never",
    "sso:staging": "aws sso login --profile apoth-staging",
    "synth": "cdk synth",
    "synth:staging": "AWS_PROFILE=apoth-staging CDK_DEFAULT_ACCOUNT=329425487030 CDK_DEFAULT_REGION=us-east-1 cdk synth --context stage=staging Apoth-staging-ServerlessPlatform",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1063.0",
    "aws-cdk-lib": "^2.258.1",
    "aws-jwt-verify": "^5.2.1",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "aws-cdk": "^2.1126.0",
    "esbuild": "^0.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
